var items;
if ((items = someObject.property).length !== 0) {
  console.log(items);
}

var anotherObject = {
  methodOne() {
    var attrs = {};

    vat test;
    if (attrs.length) {
      test = 'value';
    } else {
      test = 'another value';
    }

    return attrs;
  },

  methodTwo(data) {
    var attrs = data;

    vat test;
    if (attrs.length) {
      test = 'value';
    } else {
      test = 'another value';
    }

    return attrs;
  }
}
